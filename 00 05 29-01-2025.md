# 🐧 EC2 – Linux – Docker Setup & Basics

## 🔐 EC2 Linux SSH Access (Security Group: Port 22)

```bash
cd Downloads
chmod 400 key.pem
ssh -i "key.pem" ec2-user@ec2-107-21-179-190.compute-1.amazonaws.com
```

> ⚠️ Ensure **Port 22 (SSH)** is allowed in the EC2 Security Group from your IP.

---

## 🐳 Install Docker on Amazon Linux (EC2)

```bash
sudo yum update -y
sudo yum install docker -y
```

### Start & Enable Docker

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

### Verify Docker Installation

```bash
docker --version
sudo systemctl status docker
```

---

## 🪟 Docker Setup on Windows (Using WSL)

### Step 1: Enable WSL (PowerShell – Run as Admin)

```powershell
wsl --install
```

OR

```powershell
wsl --install -d ubuntu
```

### Step 2: Install Docker Desktop

👉 [https://docs.docker.com/desktop/setup/install/windows-install/](https://docs.docker.com/desktop/setup/install/windows-install/)

### Step 3: Verify Docker

```powershell
docker --version
docker images
```

---

## ☁️ Docker Hub Account

* **Docker Hub Username:** `atuljkamble`
* **Purpose:**

  * Pull images
  * Build images
  * Push images to Docker Hub

---

## 🔑 Docker Login

```bash
docker login
```

👉 Enter **Docker Hub username and password**

---

## 🧠 Docker Concepts (Quick Note)

* **Image** → Blueprint / Template
* **Container** → Runtime environment created from an image
* **Image + Run = Container**

---

## 📦 Docker Image & Container Commands

### List Docker Images

```bash
sudo docker images
```

### Pull Image from Docker Hub

```bash
sudo docker pull nginx:latest
```

### Run Container from Image

```bash
sudo docker run -d -p 80:80 nginx:latest
```

### Verify Running Containers

```bash
sudo docker container ls
```

---

## 🌐 Access Application

* From Browser (EC2):

  ```
  http://<instance-public-ip>
  ```
* From Local Machine:

  ```
  http://localhost:80
  ```

---

## 🔁 Run Multiple Containers (Different Ports)

```bash
sudo docker run -d -p 1000:80 nginx:latest
sudo docker run -d -p 2000:80 nginx:latest
```

```bash
sudo docker container ls
```

---

## ⏯️ Container Management

### Stop Container

```bash
sudo docker container stop <container-id>
```

### Start Container

```bash
sudo docker container start <container-id>
```

### Remove Container

```bash
sudo docker container rm <container-id>
```

---

## 🗑️ Remove Docker Image

```bash
sudo docker rmi <image-id>
```

---

## ✅ Summary

* EC2 → SSH → Install Docker
* Docker Image → Pulled from Docker Hub
* Docker Container → Running application
* Ports map host → container
* One image → multiple containers

---
